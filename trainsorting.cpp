#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> trains(n);
    vector<vector<vector<int>>> dp(n + 1, vector<vector<int>>(10001, vector<int>(10001)))
    for (int i = 0; i < n; ++i)
    {
        cin >> trains[n];
    }
    dp[0][0][10000] = 0;
    for (int i = 0; i < n; ++i)
    {
        for (int j = 0; j <= 10000; ++j)
        {
            for (int k = 0; j <= 10000; ++j)
            {
                
            }
        }
    }
}